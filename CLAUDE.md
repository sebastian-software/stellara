# CLAUDE.md

Beachte die Regeln in `AGENTS.md`. Besonders wichtig: Führe `pnpm agent:check` vor Abschluss der Aufgabe einmal vollständig aus und stelle sicher, dass alle Checks grün sind, bevor du committest oder die Aufgabe zurückgibst.

Sub-Agents (z. B. Reviewer oder Implementer) laden `AGENTS.md` nicht automatisch — wenn ein Sub-Agent Code ändert, muss er die Datei selbst lesen, damit die Quality-Gate-Regel greift.
